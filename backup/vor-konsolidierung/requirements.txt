# starlette ist explizit gepinnt, obwohl es nur eine Abhaengigkeit von fastapi ist:
# es liefert FileResponse und damit das Range-Streaming, auf dem /api/stream beruht.
# Versionen <1.3.1 haben eine quadratische Laufzeit beim Parsen von Range-Headern
# (PYSEC-2026-1942) — eine einzelne Anfrage blockiert den Event-Loop minutenlang.
fastapi==0.141.1
starlette==1.6.0
mutagen==1.48.1

# uvicorn ohne [standard]: die Extras ziehen websockets, watchfiles, python-dotenv
# und PyYAML mit, die diese App nicht benutzt. uvloop und httptools bringen den
# Performance-Vorteil und werden von uvicorn automatisch erkannt, wenn installiert.
uvicorn==0.52.1
uvloop==0.22.1
httptools==0.8.0
